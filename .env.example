# PropIQ frontend configuration.
# Copy to .env — every value below is a default.

# Base URL of the PropIQ API.
# The dashboard falls back to its bundled demo dataset if this is unreachable,
# so the app still renders with no backend running.
VITE_API_URL=http://localhost:8000
